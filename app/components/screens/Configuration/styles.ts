import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        gap: 20,
        paddingTop: 50
    },
    header: {
        marginLeft: 'auto',
    },
    alert: {
        backgroundColor: '#b00',
        borderRadius: 10,
        paddingHorizontal: 50,
        paddingVertical: 15
    },
	body: {
		gap: 15,
		width: '70%'
	}
})