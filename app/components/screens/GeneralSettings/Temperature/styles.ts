import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        gap: 50,
        alignItems: 'center',
        position: 'relative'
    },
    form: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 20
    }
})